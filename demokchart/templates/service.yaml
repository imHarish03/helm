apiVersion: v1
kind: Service
metadata:
  name: {{ include "demokchart.fullname" . }}
  labels:
    {{- include "demokchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demokchart.selectorLabels" . | nindent 4 }}
